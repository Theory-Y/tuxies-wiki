# OVERRIDE cleanup — 2026-07-12

Source: `OVERRIDE.md` (3 groups). Execution: player-coach (skip council).

## Items

### 1. NVIDIA link — `docs/notes/linux-guides/fedora.md:23`
Replace:
`- [NVIDIA Drivers Guide](https://rpmfusion.org/Howto/NVIDIA) by Official RPM Fusion.`
with:
`- [NVIDIA GPU Driver Installation](https://docs.fedoraproject.org/en-US/gaming/drivers/) by the Fedora Docs.`
- Owner: main. Done: line updated, old rpmfusion link gone.

### 2. Discord/Vesktop migration (blast radius)
- Create `docs/guides/discord-vesktop.md` from `popular.md` body (Discord, Vencord, DRP, Vesktop).
  - Frontmatter: title `Discord & Vesktop`, permalink `/guides/discord-vesktop/`, createTime kept, tags `[Beginner, Apps]`, contributors `aier`, `Lunear`.
- Delete `docs/notes/linux-apps/popular.md`.
- Remove `Popular` item from `docs/.vuepress/navbar.ts` (Apps Library group).
- Remove `Popular` card + `[Download as needed.](/linux-apps/popular/)` line from `docs/notes/linux-apps/README.md`.
- Verify no other refs to `/linux-apps/popular/` (grep clean besides above).
- Owner: main. Done: guide renders path exists, popular.md gone, 0 residual `/linux-apps/popular/` refs.

### 3. Extensions — `docs/guides/gnome-further-customisation.md` (delegated, sonnet-high)
- **Auto Power Profile**: already present (line 87) — NO-OP, do not duplicate.
- **Battery Health Charging**: add entry in alphabetical slot BETWEEN `Auto Power Profile` and `Blur my Shell`:
  ```
  - [Battery Health Charging](https://extensions.gnome.org/extension/5724/battery-health-charging/)

    :::info Set a battery charging threshold / limit / mode to maximise laptop battery lifespan. Many laptop brands work out of the box; some need extra kernel modules (see the extension's compatibility list).
    :::

    ![Battery Health Charging Demonstration](/assets/gnome-further-customisation/battery-health-limit-demonstration.png)
  ```
- **Copyous**: add a dependency install block INSIDE the existing Copyous entry, before/above the existing `:::tip Windows-like <Super>v` config block. Use `:::tabs` distro block (match repo convention `::devicon:fedora::` etc). Verified deps (Libgda w/ SQLite + GSound):
  - Fedora: `sudo dnf install libgda libgda-sqlite`
  - Arch: `sudo pacman -S libgda6`
  - Ubuntu/Debian: `sudo apt install gir1.2-gda-5.0 gir1.2-gsound-1.0`
  - openSUSE: `sudo zypper install libgda-6_0-sqlite typelib-1_0-Gda-6_0 typelib-1_0-GSound-1_0`
  - Wrap in a `:::tip Install dependencies (needed for clipboard history / SQLite backend)` so entry-order convention holds (link → info → tips/config → image last).
- Follow guidelines.md entry order: `link _(notes)_` → `:::info` → tips/config → image last.
- Done: both edits landed, format matches siblings, image renders, no dup Auto Power Profile.

### 4. Finalise (main)
- Update `ROADMAP.md` Completed; update subdir READMEs if touched; delete `OVERRIDE.md`.
- `graphify update .` if CLI available (likely not — content-only anyway).
